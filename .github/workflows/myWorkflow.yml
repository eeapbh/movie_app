name: my workflow
on: [push]

jobs:
  build:
    name: hello github action
    runs-on: ubuntu-18.04
    steps:
      - name: checout source code
        uses: actions/checkout@master

      - name: echo hello
        run: echo "Hello"
